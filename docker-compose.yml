version: '3.8'

services:
  swiggy-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: swiggy-unified-app:latest
    ports:
      - "8000:8000"
    environment:
      - ENV=production
    restart: unless-stopped
